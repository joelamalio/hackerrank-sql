SELECT * FROM (SELECT CITY, LENGTH(CITY) AS LEN FROM STATION GROUP BY CITY ORDER BY LEN, CITY) WHERE ROWNUM = 1;
SELECT * FROM (SELECT CITY, LENGTH(CITY) AS LEN FROM STATION GROUP BY CITY ORDER BY LEN DESC, CITY) WHERE ROWNUM = 1;